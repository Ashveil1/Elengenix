"""elengenix.reports — Report generation subsystem (markdown, PDF, export, CVSS).

Provides:
- cvss: CVSS v3.1 vector parsing, score calculation, severity labeling
- markdown: Markdown report assembly from flow/task/subtask data
- pdf: PDF rendering from markdown
- export: Multi-format export (md, pdf, html, json)
- templates: Jinja2-style report templates
"""
