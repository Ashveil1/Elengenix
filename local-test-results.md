# Elengenix Local Test Run Results

**Date:** 2026-07-27
**Working dir:** `/home/z/my-project/elengenix-ci-fix`
**Command run (CI-equivalent):**
```bash
python3 -m pytest tests/ -m "not integration" \
    --ignore=tests/test_brain_coverage.py \
    --ignore=tests/test_brain_coverage_gap.py \
    --timeout=300 --tb=short -q
```

## 1. Summary Counts

| Metric | Count |
|---|---|
| **Total tests collected** | 3006 |
| **Passed** | 2969 |
| **Failed** | 37 |
| **Errors (collection)** | 0 |
| **Timeouts** | 0 |
| **Warnings** | 2 |
| **Total runtime** | ~63 s (61.24 s test phase + ~8.9 s collection) |

**Note on flakiness:** A second run with `--timeout=60` produced **38 failed** instead of 37 — the extra failure was `tests/test_agent_tools.py::TestSearchFiles::test_search_no_match` (`assert r["success"]` failed because searching `/tmp` returned `success=False` while `/tmp` was full of `pip-unpack-*` directories). After cleaning `/tmp`, the same test passes. This test is environment-sensitive, not a code regression.

## 2. Failed Tests (full list with error messages)

All 37 failures live in **two files only**:
- `tests/brutal/test_integration_security_brutal.py` — 36 failures
- `tests/test_agent_tools.py` — 1 failure (only in the `--timeout=60` run; passes with `--timeout=300`)

The 36 `test_integration_security_brutal.py` failures cluster in four classes: `TestReports` (27), `TestSecurity` (3), `TestStressPerformance` (5), `TestEndToEndIntegration` (1).

### 2.1 `TestEndToEndIntegration::test_report_generation_multi_format_export`
**File/line:** `tests/brutal/test_integration_security_brutal.py:514`
**Error:**
```
data = await export_report(11, fmt, provider=_Provider())
E   TypeError: export_report() got an unexpected keyword argument 'provider'
```
**Root cause:** `elengenix.reports.export.export_report` has signature `(flow, tasks, subtasks=None, fmt='md') -> bytes`. Tests expect `(flow_id, fmt, provider=Provider) -> bytes` (provider fetches flow/tasks).

### 2.2 `TestReports::test_generate_report_markdown_flow_with_zero_tasks`
**File/line:** `tests/brutal/test_integration_security_brutal.py:1276`
**Error:**
```
assert "No tasks available" in md
E   AssertionError: assert 'No tasks available' in '# empty\n'
```
**Root cause:** `generate_report_markdown` does not emit a "No tasks available" placeholder when the task list is empty.

### 2.3 `TestReports::test_generate_report_markdown_one_task_zero_subtasks`
**File/line:** `tests/brutal/test_integration_security_brutal.py:1287`
**Error:**
```
assert "## Table of Contents" in md
E   AssertionError: assert '## Table of Contents' in '# one\n\n## 📋 only\n\n**Input:**\n```\ni\n```\n\n**Result:**\nr\n'
```
**Root cause:** TOC section not generated by `generate_report_markdown`.

### 2.4 `TestReports::test_generate_report_markdown_toc_generation`
**File/line:** `tests/brutal/test_integration_security_brutal.py:1320`
**Error:**
```
assert "- [" in toc
E   AssertionError: assert '- [' in ''
```
**Root cause:** TOC builder returns empty string (no `- [ ... ]` entries).

### 2.5 `TestReports::test_generate_report_markdown_anchor_ids_github_slugger_compatible`
**File/line:** `tests/brutal/test_integration_security_brutal.py:1329`
**Error:**
```
assert slugify_github("Café ☕ Table") == "caf-table"
E   AssertionError: assert 'café-table' == 'caf-table'
E     - caf-table
E     + café-table
E       ?    +
```
**Root cause:** `slugify_github` lowercases and strips emoji/whitespace but does **not** strip accented characters (é). Expected behaviour matches GitHub's slugger: ASCII-only.

### 2.6 `TestReports::test_generate_report_markdown_status_emojis`
**File/line:** `tests/brutal/test_integration_security_brutal.py:1338`
**Error:**
```
assert status_emoji("created") == "\U0001F4DD"  # 📝
E   AssertionError: assert '📋' == '📝'
```
**Root cause:** `status_emoji("created")` returns `📋` (the value mapped to `"todo"`), but tests expect the dedicated "created" emoji `📝`. Likely the `created` key is missing and falls back to `todo`.

### 2.7 `TestReports::test_generate_report_markdown_header_shifting_h1_to_h4`
**File/line:** `tests/brutal/test_integration_security_brutal.py:1355`
**Error:**
```
assert "#### Big Heading" in md
E   AssertionError: assert '#### Big Heading' in '# shift\n\n## 📋 t\n\n**Input:**\n```\n# Big Heading\n\nbody\n```\n'
```
**Root cause:** `shift_markdown_headers` is not shifting `# Big Heading` inside a code block to `#### Big Heading` (the input header is preserved verbatim inside the fenced code block — but the test expects shift-by-3 to apply to the rendered body). The function appears to skip the markdown body or is not being invoked from `generate_report_markdown`.

### 2.8 `TestReports::test_render_to_pdf_basic_markdown`
**File/line:** `tests/brutal/test_integration_security_brutal.py:1414`
**Error:**
```
assert len(pdf) > 1000
E   AssertionError: assert 618 > 1000
E    +  where 618 = len(b'%PDF-1.4\n1 0 obj\n<< /Type /Catalog /Pages 2 0 R >>\nendobj\n2 0 obj\n<< /Type /Pages /Kids [3 0 R] /Count 1 >>\n...')
```
**Root cause:** The generated PDF is only 618 bytes — i.e. it has an empty page, no text content. The markdown content is not being written into the PDF.

### 2.9 `TestReports::test_render_to_pdf_with_cjk_content`
**File/line:** `tests/brutal/test_integration_security_brutal.py:1451`
**Error:**
```
assert len(pdf) > 5000
E   AssertionError: assert 620 > 5000
```
**Root cause:** Same as 2.8 — PDF is empty (620 bytes vs expected 5000+ for CJK content with font subset).

### 2.10 `TestReports::test_render_to_pdf_emoji_substitution`
**File/line:** `tests/brutal/test_integration_security_brutal.py:1458`
**Error:**
```
assert len(EMOJI_SUBSTITUTIONS) == 16
E   AssertionError: assert 29 == 16
E    +  where 29 = len({'⏳': '[WAIT]', '⚔️': '[SWORD]', '⚠️': '[WARN]', '✅': '[OK]', ...})
```
**Root cause:** `EMOJI_SUBSTITUTIONS` table in `elengenix/reports/pdf.py` has 29 entries; test expects exactly 16. Either the test is outdated or the substitution table was over-populated.

### 2.11 `TestReports::test_render_to_pdf_heading_styles_h1_16pt_h2_14pt`
**File/line:** `tests/brutal/test_integration_security_brutal.py:1469`
**Error:**
```
assert HEADING_FONT_SIZES[1] == 16
E   assert 24.0 == 16
```
**Root cause:** `HEADING_FONT_SIZES[1]` is `24.0` but test expects `16`. Full actual map: `{1: 24.0, 2: 20.0, 3: 16.0, 4: 14.0, 5: 12.0, 6: 11.0}`. Test expects H1=16, H2=14 (i.e. much smaller).

### 2.12 `TestReports::test_split_by_cjk_alternating_segments`
**File/line:** `tests/brutal/test_integration_security_brutal.py:1493`
**Error:**
```
assert segs[0].is_cjk is False and segs[0].text == "hello "
E   AttributeError: 'str' object has no attribute 'is_cjk'
```
**Root cause:** `split_by_cjk` (or whatever the test imports) returns plain strings, but tests expect objects with `.is_cjk` and `.text` attributes (a dataclass/NamedTuple).

### 2.13 `TestReports::test_split_by_cjk_empty_returns_single_empty_segment`
**File/line:** `tests/brutal/test_integration_security_brutal.py:1502`
**Error:**
```
assert len(segs) == 1
E   assert 0 == 1
E    +  where 0 = len([])
```
**Root cause:** Empty input returns `[]` instead of a single empty segment.

### 2.14 `TestReports::test_split_by_cjk_pure_cjk_input`
**File/line:** `tests/brutal/test_integration_security_brutal.py:1512`
**Error:**
```
assert segs[0].is_cjk is True
E   AttributeError: 'str' object has no attribute 'is_cjk'
```
**Root cause:** Same as 2.12 — return type mismatch.

### 2.15 `TestReports::test_vulnerability_template_all_sections_present`
**File/line:** `tests/brutal/test_integration_security_brutal.py:1612`
**Error:**
```
assert "{" + ph + "}" in VULNERABILITY_TEMPLATE, f"missing {ph}"
E   AssertionError: missing cve_id
```
**Root cause:** `VULNERABILITY_TEMPLATE` in `elengenix/reports/templates.py` has placeholders `{title}`, `{cvss_score}`, `{severity}`, `{cvss_vector}`, `{description}`, `{affected}`, `{poc}`, `{remediation}`, `{references}` but is missing `{cve_id}`.

### 2.16 `TestReports::test_executive_summary_template_sections`
**File/line:** `tests/brutal/test_integration_security_brutal.py:1618`
**Error:**
```
assert "engagement_name" in EXECUTIVE_SUMMARY_TEMPLATE
E   AssertionError: assert 'engagement_name' in '# Executive Summary — {title}\n\n## Overview\n{overview}\n\n## Key Findings\n{findings}\n\n## Recommendations\n{recommendations}\n\n## Risk Assessment\n{risk_assessment}\n'
```
**Root cause:** `EXECUTIVE_SUMMARY_TEMPLATE` is missing `engagement_name` placeholder.

### 2.17 `TestReports::test_technical_report_template_sections`
**File/line:** `tests/brutal/test_integration_security_brutal.py:1633`
**Error:**
```
assert s in TECHNICAL_REPORT_TEMPLATE
E   AssertionError: assert 'overview' in '# Technical Report — {title}\n\n## Scope\n{scope}\n\n## Methodology\n{methodology}\n\n## Detailed Findings\n{findings}\n\n## Technical Details\n{technical_details}\n\n## Remediation\n{remediation}\n'
```
**Root cause:** `TECHNICAL_REPORT_TEMPLATE` is missing `overview` placeholder.

### 2.18 `TestReports::test_compliance_report_template_pci_soc2_iso27001`
**File/line:** `tests/brutal/test_integration_security_brutal.py:1639`
**Error:**
```
assert "PCI-DSS" in COMPLIANCE_REPORT_TEMPLATE
E   AssertionError: assert 'PCI-DSS' in '# Compliance Report — {title}\n\n## Compliance Status\n{status}\n\n## Controls Assessed\n{controls}\n\n## Findings\n{findings}\n\n## Gap Analysis\n{gap_analysis}\n\n## Remediation Plan\n{remediation_plan}\n'
```
**Root cause:** `COMPLIANCE_REPORT_TEMPLATE` is missing `PCI-DSS`, `SOC2`, `ISO27001` literals.

### 2.19 `TestReports::test_export_report_markdown_format`
**File/line:** `tests/brutal/test_integration_security_brutal.py:1661`
**Error:**
```
data = await export_report(1, "markdown", provider=_P())
E   TypeError: export_report() got an unexpected keyword argument 'provider'
```
**Root cause:** Same as 2.1 — signature mismatch.

### 2.20 `TestReports::test_export_report_html_format`
**File/line:** `tests/brutal/test_integration_security_brutal.py:1677`
**Error:**
```
data = await export_report(1, "html", provider=_P())
E   TypeError: export_report() got an unexpected keyword argument 'provider'
```
**Root cause:** Same as 2.1.

### 2.21 `TestReports::test_export_report_json_format`
**File/line:** `tests/brutal/test_integration_security_brutal.py:1693`
**Error:**
```
data = await export_report(1, "json", provider=_P())
E   TypeError: export_report() got an unexpected keyword argument 'provider'
```
**Root cause:** Same as 2.1.

### 2.22 `TestReports::test_export_report_pdf_format`
**File/line:** `tests/brutal/test_integration_security_brutal.py:1712`
**Error:**
```
data = await export_report(1, "pdf", provider=_P())
E   TypeError: export_report() got an unexpected keyword argument 'provider'
```
**Root cause:** Same as 2.1.

### 2.23 `TestReports::test_generate_filename_pattern`
**File/line:** `tests/brutal/test_integration_security_brutal.py:1720`
**Error:**
```
name = await generate_filename(42, "Pentest Report!", "pdf")
E   TypeError: generate_filename() takes 2 positional arguments but 3 were given
```
**Root cause:** `generate_filename` accepts only 2 args (likely `flow_id, fmt`), tests pass 3 (`flow_id, title, fmt`).

### 2.24 `TestReports::test_generate_filename_unknown_format_defaults_txt`
**File/line:** `tests/brutal/test_integration_security_brutal.py:1729`
**Error:**
```
name = await generate_filename(1, "title", "docx")
E   TypeError: generate_filename() takes 2 positional arguments but 3 were given
```
**Root cause:** Same as 2.23.

### 2.25 `TestReports::test_report_anchors_with_duplicate_headings_get_suffix`
**File/line:** `tests/brutal/test_integration_security_brutal.py:1743`
**Error:**
```
anchors = generate_anchors(["Intro", "Intro", "Intro", "Outro"])
elengenix/reports/markdown.py:35: in generate_anchors
    return f"#{slugify_github(title)}"
elengenix/reports/markdown.py:25: in slugify_github
    slug = text.lower().strip()
E   AttributeError: 'list' object has no attribute 'lower'
```
**Root cause:** `generate_anchors(title: str)` takes a single string; tests call it with a list of strings (and expect a list of anchors with `-1`, `-2`, etc. suffixes for duplicates).

### 2.26 `TestReports::test_report_default_status_emoji_for_unknown`
**File/line:** `tests/brutal/test_integration_security_brutal.py:1758`
**Error:**
```
assert DEFAULT_STATUS_EMOJI == "\U0001F4DD"
E   AssertionError: assert {'cancelled': '🚫', 'failed': '❌', 'finished': '✅', 'pending': '⏳', ...} == '📝'
```
**Root cause:** `DEFAULT_STATUS_EMOJI` in `elengenix/reports/markdown.py` is the **whole emoji dict** (`{'finished': '✅', 'running': '🔄', ...}`), but tests expect it to be the single fallback emoji `'📝'`. Naming collision — the constant name refers to a fallback, not a dict.

### 2.27 `TestReports::test_report_export_unsupported_format_raises_value_error`
**File/line:** `tests/brutal/test_integration_security_brutal.py:1779`
**Error:**
```
await export_report(1, "docx", provider=_P())
E   TypeError: export_report() got an unexpected keyword argument 'provider'
```
**Root cause:** Same as 2.1.

### 2.28 `TestReports::test_report_render_html_with_pygments_highlight`
**File/line:** `tests/brutal/test_integration_security_brutal.py:1786`
**Error:**
```
html = render_html(md, include_css=True)
E   TypeError: render_html() got an unexpected keyword argument 'include_css'
```
**Root cause:** There is no `render_html` in `elengenix.reports.markdown`. The test imports something named `render_html` that does exist (perhaps in another module) but it does not accept `include_css`. Need to verify the import — likely the function exists in a different module or with a different signature.

### 2.29 `TestReports::test_report_render_template_substitutes_missing_keys_with_empty`
**File/line:** `tests/brutal/test_integration_security_brutal.py:1794`
**Error:**
```
out = render_template(VULNERABILITY_TEMPLATE, {"cve_id": "CVE-2024-1"})
E   TypeError: render_template() takes 1 positional argument but 2 were given
```
**Root cause:** `render_template()` only takes 1 positional arg (likely just the template), tests expect `render_template(template, mapping)` that substitutes `{key}` placeholders.

### 2.30 `TestSecurity::test_xss_script_tag_in_input_is_escaped_in_html_export`
**File/line:** `tests/brutal/test_integration_security_brutal.py:1959`
**Error:**
```
html = render_html(md, include_css=False)
E   TypeError: render_html() got an unexpected keyword argument 'include_css'
```
**Root cause:** Same as 2.28.

### 2.31 `TestSecurity::test_xss_img_onerror_in_input_is_escaped_in_html_export`
**File/line:** `tests/brutal/test_integration_security_brutal.py:1970`
**Error:**
```
html = render_html(md, include_css=False)
E   TypeError: render_html() got an unexpected keyword argument 'include_css'
```
**Root cause:** Same as 2.28.

### 2.32 `TestSecurity::test_xss_javascript_url_in_markdown_link_is_not_active`
**File/line:** `tests/brutal/test_integration_security_brutal.py:1981`
**Error:**
```
html = render_html(md, include_css=False)
E   TypeError: render_html() got an unexpected keyword argument 'include_css'
```
**Root cause:** Same as 2.28.

### 2.33 `TestStressPerformance::test_reports_export_markdown_under_1s_for_100_tasks`
**File/line:** `tests/brutal/test_integration_security_brutal.py:2921`
**Error:**
```
data = await export_report(1, "markdown", provider=_P())
E   TypeError: export_report() got an unexpected keyword argument 'provider'
```
**Root cause:** Same as 2.1.

### 2.34 `TestStressPerformance::test_reports_export_json_under_1s_for_100_tasks`
**File/line:** `tests/brutal/test_integration_security_brutal.py:2943`
**Error:**
```
data = await export_report(1, "json", provider=_P())
E   TypeError: export_report() got an unexpected keyword argument 'provider'
```
**Root cause:** Same as 2.1.

### 2.35 `TestStressPerformance::test_stress_random_bytes_in_cvss_parse_rejected_gracefully`
**File/line:** `tests/brutal/test_integration_security_brutal.py:2987`
**Error:**
```
with pytest.raises((ValueError, UnicodeDecodeError)):
E   Failed: DID NOT RAISE any of (<class 'ValueError'>, <class 'UnicodeDecodeError'>)
```
**Root cause:** `parse_cvss_vector` in `elengenix/reports/cvss.py` does not raise `ValueError` on garbage input — it silently returns a default `CVSSVector` instead. Tests expect strict validation.

### 2.36 `TestStressPerformance::test_stress_50_concurrent_cvss_calculations_distinct_vectors`
**File/line:** `tests/brutal/test_integration_security_brutal.py:3012`
**Error:**
```
CVSSVector(
E   TypeError: CVSSVector.__init__() got an unexpected keyword argument 'confidentiality_impact'
```
**Root cause:** `CVSSVector.__init__` uses kwargs `confidentiality`, `integrity`, `availability` — but tests pass `confidentiality_impact`, `integrity_impact`, `availability_impact`.

### 2.37 `TestStressPerformance::test_stress_500_render_html_calls_under_3s`
**File/line:** `tests/brutal/test_integration_security_brutal.py:3073`
**Error:**
```
render_html(md, include_css=False)
E   TypeError: render_html() got an unexpected keyword argument 'include_css'
```
**Root cause:** Same as 2.28.

## 3. Collection Errors (import errors)

**None.** All 3006 tests collected successfully. The earlier `ModuleNotFoundError: No module named 'aiosqlite'` was resolved by installing `aiosqlite`, `httpx`, `pydantic`, `pyyaml`, `aiohttp`, `starlette`, `uvicorn`, `fastapi`, `textual`, `trafilatura`, `replicate` before running.

## 4. Categorization

| Category | Count | Notes |
|---|---|---|
| **Import errors (collection)** | 0 | All modules imported fine |
| **TypeError — signature mismatch** | 17 | Tests call functions with kwargs/args the implementation does not accept |
| **AttributeError — wrong return type** | 3 | `split_by_cjk` returns `str` not dataclass; `generate_anchors` takes `str` not `list` |
| **AssertionError — content/value mismatch** | 16 | Templates missing placeholders, PDF too small, wrong emoji, wrong font size, missing TOC, etc. |
| **DID NOT RAISE** | 1 | `parse_cvss_vector` swallows invalid input instead of raising |
| **Timeouts** | 0 | No tests hit the 300 s per-test timeout |
| **Flaky / environment-sensitive** | 1 | `test_search_no_match` (passes after `/tmp` cleanup) |

### Failure clusters by root cause

1. **`export_report(flow_id, fmt, provider=...)` vs `(flow, tasks, subtasks, fmt)`** — 8 failures (2.1, 2.19, 2.20, 2.21, 2.22, 2.27, 2.33, 2.34). Tests expect a high-level "fetch-and-render" API; implementation is low-level "render-already-fetched".

2. **`render_html(md, include_css=...)` not implemented / signature differs** — 5 failures (2.28, 2.30, 2.31, 2.32, 2.37).

3. **`generate_filename(flow_id, title, fmt)` — 3 args expected, 2 given** — 2 failures (2.23, 2.24).

4. **`render_template(template, mapping)` — 2 args expected, 1 given** — 1 failure (2.29).

5. **`CVSSVector(confidentiality_impact=...)` kwarg renamed to `confidentiality`** — 1 failure (2.36).

6. **`parse_cvss_vector` does not raise on garbage** — 1 failure (2.35).

7. **`generate_anchors` takes `list[str]` not `str`** — 1 failure (2.25).

8. **`split_by_cjk` returns `str` not `Segment` dataclass** — 2 failures (2.12, 2.14); plus empty-input edge case (2.13).

9. **`slugify_github` does not strip accented chars** — 1 failure (2.5).

10. **`status_emoji("created")` returns wrong emoji** — 1 failure (2.6).

11. **`DEFAULT_STATUS_EMOJI` is dict not single emoji** — 1 failure (2.26).

12. **Templates missing placeholders** (`cve_id`, `engagement_name`, `overview`, `PCI-DSS`) — 4 failures (2.15, 2.16, 2.17, 2.18).

13. **`generate_report_markdown` missing TOC, "No tasks available", header shifting** — 4 failures (2.2, 2.3, 2.4, 2.7).

14. **PDF renderer produces empty/undersized output** — 2 failures (2.8, 2.9).

15. **PDF constants mismatched** (`EMOJI_SUBSTITUTIONS=29 vs 16`, `HEADING_FONT_SIZES[1]=24 vs 16`) — 2 failures (2.10, 2.11).

## 5. Environment Notes

- Python: `3.12.13`
- Working directory: `/home/z/my-project/elengenix-ci-fix`
- The `pip install -e .` step pulled many deps; `sentence-transformers` install aborted due to "No space left on device" (cleaned `/tmp/pip-unpack-*` and `~/.cache/pip/*` to recover ~6 GB). Test suite does not actually require `sentence-transformers` to run.
- `chromadb`, `nest-asyncio`, `networkx`, `tiktoken` were **not** installed (suite completed without them — they are not on the import path of the tests we ran).
- 2 warnings:
  - `tests/test_scanning_executor.py::TestExecuteToolRegistry::test_tool_registry_exception_falls_back`
  - `tests/test_scanning_helpers.py::TestGetMemoryProfileContext::test_no_profile`
  - Both are `RuntimeWarning: coroutine 'execute_tool_registry.<locals>._run' was never awaited` (a mock returning a coroutine that was never awaited). Not failures, just hygiene issues.

## 6. Next Actions

1. **Reconcile `elengenix.reports.export.export_report` API** — either add a `provider`-based overload that fetches flow/tasks itself, or update the 8 tests to call the low-level form. Recommend adding the high-level overload since 8 tests depend on it.
2. **Implement `render_html(md, include_css=False)` in `elengenix.reports.markdown`** (5 tests depend on it).
3. **Implement `render_template(template, mapping)` with `{key}` substitution** (1 test).
4. **Update `generate_filename` to accept `(flow_id, title, fmt)`** (2 tests).
5. **Rename `CVSSVector` kwargs** `confidentiality_impact`, `integrity_impact`, `availability_impact` → keep backward-compat aliases (1 test).
6. **Make `parse_cvss_vector` raise `ValueError` on invalid input** (1 test).
7. **Refactor `generate_anchors` to accept `list[str]` and produce de-duplicated anchor list** (1 test).
8. **Refactor `split_by_cjk` to return `Segment` objects with `.is_cjk` and `.text`** (3 tests).
9. **Update `slugify_github` to strip non-ASCII** (use `unicodedata.normalize('NFKD', ...).encode('ascii', 'ignore').decode()`).
10. **Add `created` key to status-emoji map OR fix `status_emoji("created")` to return `📝`**.
11. **Rename the dict `DEFAULT_STATUS_EMOJI` → `STATUS_EMOJIS` and add `DEFAULT_STATUS_EMOJI = "📝"` constant** (1 test).
12. **Add missing template placeholders**: `cve_id`, `engagement_name`, `overview`, `PCI-DSS`/`SOC2`/`ISO27001` (4 tests).
13. **Add TOC generation, "No tasks available" placeholder, header-shifting in `generate_report_markdown`** (4 tests).
14. **Fix PDF renderer to actually write content** (currently produces empty 618-byte PDFs) (2 tests).
15. **Reconcile PDF constants** with test expectations: `EMOJI_SUBSTITUTIONS` to exactly 16 entries, `HEADING_FONT_SIZES[1]=16, [2]=14` (2 tests).

## 7. Raw pytest output (tail)

```
=============================== warnings summary ===============================
tests/test_scanning_executor.py::TestExecuteToolRegistry::test_tool_registry_exception_falls_back
tests/test_scanning_helpers.py::TestGetMemoryProfileContext::test_no_profile
  /home/z/.local/share/uv/python/cpython-3.12.13-linux-x86_64-gnu/lib/python3.12/unittest/mock.py:2217: RuntimeWarning: coroutine 'execute_tool_registry.<locals>._run' was never awaited

-- Docs: https://docs.pytest.org/en/stable/how-to/capture-warnings.html#resource-warnings for more information.
=========================== short test summary info ============================
FAILED tests/brutal/test_integration_security_brutal.py::TestEndToEndIntegration::test_report_generation_multi_format_export
FAILED tests/brutal/test_integration_security_brutal.py::TestReports::test_generate_report_markdown_flow_with_zero_tasks
FAILED tests/brutal/test_integration_security_brutal.py::TestReports::test_generate_report_markdown_one_task_zero_subtasks
FAILED tests/brutal/test_integration_security_brutal.py::TestReports::test_generate_report_markdown_toc_generation
FAILED tests/brutal/test_integration_security_brutal.py::TestReports::test_generate_report_markdown_anchor_ids_github_slugger_compatible
FAILED tests/brutal/test_integration_security_brutal.py::TestReports::test_generate_report_markdown_status_emojis
FAILED tests/brutal/test_integration_security_brutal.py::TestReports::test_generate_report_markdown_header_shifting_h1_to_h4
FAILED tests/brutal/test_integration_security_brutal.py::TestReports::test_render_to_pdf_basic_markdown
FAILED tests/brutal/test_integration_security_brutal.py::TestReports::test_render_to_pdf_with_cjk_content
FAILED tests/brutal/test_integration_security_brutal.py::TestReports::test_render_to_pdf_emoji_substitution
FAILED tests/brutal/test_integration_security_brutal.py::TestReports::test_render_to_pdf_heading_styles_h1_16pt_h2_14pt
FAILED tests/brutal/test_integration_security_brutal.py::TestReports::test_split_by_cjk_alternating_segments
FAILED tests/brutal/test_integration_security_brutal.py::TestReports::test_split_by_cjk_empty_returns_single_empty_segment
FAILED tests/brutal/test_integration_security_brutal.py::TestReports::test_split_by_cjk_pure_cjk_input
FAILED tests/brutal/test_integration_security_brutal.py::TestReports::test_vulnerability_template_all_sections_present
FAILED tests/brutal/test_integration_security_brutal.py::TestReports::test_executive_summary_template_sections
FAILED tests/brutal/test_integration_security_brutal.py::TestReports::test_technical_report_template_sections
FAILED tests/brutal/test_integration_security_brutal.py::TestReports::test_compliance_report_template_pci_soc2_iso27001
FAILED tests/brutal/test_integration_security_brutal.py::TestReports::test_export_report_markdown_format
FAILED tests/brutal/test_integration_security_brutal.py::TestReports::test_export_report_html_format
FAILED tests/brutal/test_integration_security_brutal.py::TestReports::test_export_report_json_format
FAILED tests/brutal/test_integration_security_brutal.py::TestReports::test_export_report_pdf_format
FAILED tests/brutal/test_integration_security_brutal.py::TestReports::test_generate_filename_pattern
FAILED tests/brutal/test_integration_security_brutal.py::TestReports::test_generate_filename_unknown_format_defaults_txt
FAILED tests/brutal/test_integration_security_brutal.py::TestReports::test_report_anchors_with_duplicate_headings_get_suffix
FAILED tests/brutal/test_integration_security_brutal.py::TestReports::test_report_default_status_emoji_for_unknown
FAILED tests/brutal/test_integration_security_brutal.py::TestReports::test_report_export_unsupported_format_raises_value_error
FAILED tests/brutal/test_integration_security_brutal.py::TestReports::test_report_render_html_with_pygments_highlight
FAILED tests/brutal/test_integration_security_brutal.py::TestReports::test_report_render_template_substitutes_missing_keys_with_empty
FAILED tests/brutal/test_integration_security_brutal.py::TestSecurity::test_xss_script_tag_in_input_is_escaped_in_html_export
FAILED tests/brutal/test_integration_security_brutal.py::TestSecurity::test_xss_img_onerror_in_input_is_escaped_in_html_export
FAILED tests/brutal/test_integration_security_brutal.py::TestSecurity::test_xss_javascript_url_in_markdown_link_is_not_active
FAILED tests/brutal/test_integration_security_brutal.py::TestStressPerformance::test_reports_export_markdown_under_1s_for_100_tasks
FAILED tests/brutal/test_integration_security_brutal.py::TestReports::test_reports_export_json_under_1s_for_100_tasks
FAILED tests/brutal/test_integration_security_brutal.py::TestStressPerformance::test_stress_random_bytes_in_cvss_parse_rejected_gracefully
FAILED tests/brutal/test_integration_security_brutal.py::TestStressPerformance::test_stress_50_concurrent_cvss_calculations_distinct_vectors
FAILED tests/brutal/test_integration_security_brutal.py::TestStressPerformance::test_stress_500_render_html_calls_under_3s
=========================== 37 failed, 2969 passed, 2 warnings in 61.24s (0:01:01) ============================
```
